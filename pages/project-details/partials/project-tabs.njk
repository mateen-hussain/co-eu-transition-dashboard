{{ govukTabs({
  items: [
    {
      label: "Project description",
      id: "project-description",
      panel: {
        html: "<p class='govuk-body'>" + project.description | default('N/A') + "</p>" | safe
      }
    },
    {
      label: "Project solution",
      id: "project-solution",
      panel: {
        html: "<p class='govuk-body'>" + project.fields.get('solution').value| default('N/A') + "</p>" | safe
      }
    },
    {
      label: "Contact details",
      id: "contact-details",
      panel: {
        html: "<p class='govuk-body'>" + project.sro | default('N/A') + "</p>"
      }
    }
  ]
}) }}