          {{ govukCheckboxes({
              name: "filters[" + filter.id + "][]",
              items: [
                {
                  value: option.value,
                  text: option.value + "(" + option.count + ")",
                  checked: option.value in data.filters[filter.id] | default([])
                }
              ]
            }) }}