          {{ govukCheckboxes({
              name: "filters[projects][" + filter.id + "][]",
              items: [
                {
                  value: option.value,
                  text: option.value + (" (" + option.count + ")" if option.count else ""),
                  checked: option.value in data.filters.projects[filter.id] | default([])
                }
              ]
            }) }}